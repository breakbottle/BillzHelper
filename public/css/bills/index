@import "../vars"

.bill-details{
  margin:10px;
  padding:10px;
  border-radius: 5px;
  background-color: $color-two;
  border:1px solid $color-one;
}
.creditor-title{
  text-transform capitalize;
}
.action-content{
  overflow: hidden;
  position: relative;
  .action-content-wrap{
    min-height : 500px;
  }
}
.action-content-wrap,.bill-item,.bill-confirm{
  overflow-y: hidden;
  max-height: 500px;
  //max-width :520px;
  transition-property: all;
  transition-duration: .7s;
  transition-timing-function: cubic-bezier(1, 0.9, 0.7, 1);
  left: 0;
  top: 50px;
  width: 100%;

  &.closed{
    max-height :0;
    min-height :0;
    border:0;
    padding:0;

  }
    &.right{
      position: absolute;
      left: 100%;//520px;
    }
    &.left{
      position: absolute;
      left: -100%;//-520px;
    }
    &.reset{
      position: absolute;
      left: 0;
    }
}

